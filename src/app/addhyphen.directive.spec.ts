import { AddhyphenDirective } from './addhyphen.directive';

describe('AddhyphenDirective', () => {
  it('should create an instance', () => {
    const directive = new AddhyphenDirective();
    expect(directive).toBeTruthy();
  });
});
